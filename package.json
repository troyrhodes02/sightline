{
  "name": "sightline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prisma:validate": "prisma validate",
    "prisma:format": "prisma format",
    "test:schema": "node --test \"prisma/tests/**/*.test.mjs\""
  },
  "dependencies": {
    "@prisma/client": "^7.9.1",
    "dotenv": "^17.2.0"
  },
  "devDependencies": {
    "prisma": "^7.9.1"
  }
}
